name: cuit-thesis-docx-format
channels:
  - conda-forge
dependencies:
  - python>=3.11
  - pip
  - lxml>=5.0
  - pillow>=10.0
  - pip:
      - python-docx>=1.1.2
      - PyMuPDF>=1.24
      - pywin32>=306; platform_system == "Windows"
